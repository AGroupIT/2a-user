# Changelog v1.0.0+5

## Дата релиза: 2026-01-23

### 🎨 UI/UX улучшения

#### 1. FAB кнопка "Добавить треки"
- **Изменение**: Увеличен отступ снизу с 24px до 29px
- **Причина**: Кнопка была слишком близко прижата к нижнему меню
- **Файл**: `tracks_screen.dart:2001`

**До:**
```dart
bottom: AppLayout.bottomBarHeight +
    AppLayout.bottomBarBottomMargin +
    24,
```

**После:**
```dart
bottom: AppLayout.bottomBarHeight +
    AppLayout.bottomBarBottomMargin +
    29, // Увеличено для визуального разделения от нижнего меню
```

#### 2. Кнопка "Отправить на сборку" в модальном окне
- **Изменение**: Кнопка теперь занимает 80% ширины и центрирована
- **Причина**: Улучшение визуального восприятия
- **Файл**: `tracks_screen.dart:1372-1391`

**До:**
```dart
FilledButton(
  onPressed: ...,
  child: const Text('Отправить на сборку'),
),
```

**После:**
```dart
Center(
  child: SizedBox(
    width: MediaQuery.of(context).size.width * 0.8,
    child: FilledButton(
      onPressed: ...,
      child: const Text('Отправить на сборку'),
    ),
  ),
),
```

#### 3. Исправлен overflow в модальном окне "Создать сборку"
- **Проблема**: На маленьких экранах появлялись ошибки "bottom overflowed by XX pixels" и "right overflowed"
- **Исправления**:

**a) Типы упаковки - заменен GridView на Wrap:**
- Было: `GridView.count` с фиксированным `childAspectRatio: 3.0`
- Стало: `Wrap` с автоматическим переносом элементов
- Добавлены `constraints` для фиксированной ширины элементов
- Увеличено `maxLines` с 1 до 2 для названий типов упаковки
- Файл: `tracks_screen.dart:1151-1233`

**b) Дропдаун тарифов - исправлен overflow текста:**
- Добавлен `Expanded` вокруг Column с текстом
- Добавлено `overflow: TextOverflow.ellipsis` для обрезки длинных названий
- Добавлено `maxLines: 1` для лейбла
- Файл: `tracks_screen.dart:3456-3485`

### 📦 Все исправления из предыдущих версий:

**v1.0.0+4:**
- ✅ Исправлен автоматический logout при неверном пароле

**v1.0.0+3:**
1. ✅ FAB кнопка на Samsung A15
2. ✅ Понятные ошибки без интернета
3. ✅ Фокус в поле страховки
4. ✅ Отображение ошибок авторизации
5. ✅ Чёрный экран при добавлении треков

---

## Версия

- **Version**: 1.0.0+5
- **Build number**: 5 (была +4)
- **Форматы**: APK, AAB

---

## Для QA тестирования

### Сценарий 1: FAB кнопка "Добавить треки"
1. Открыть приложение на Samsung A15 (Android 15)
2. Перейти на экран "Треки"
3. Проскроллить вниз до конца

**Ожидаемое поведение:**
- ✅ Кнопка "Добавить треки" имеет визуальное разделение от нижнего меню
- ✅ Между кнопкой и меню видимый отступ (29px)

### Сценарий 2: Модальное окно "Создать сборку" на маленьких экранах
1. Открыть экран "Треки"
2. Выбрать несколько треков для сборки
3. Нажать кнопку групповых действий
4. Выбрать "Создать сборку"
5. Открыть модальное окно на устройстве с маленьким экраном

**Ожидаемое поведение:**
- ✅ Типы упаковки корректно отображаются без overflow
- ✅ Названия типов упаковки могут занимать 2 строки
- ✅ Длинные названия тарифов обрезаются троеточием
- ✅ Кнопка "Отправить на сборку" занимает 80% ширины экрана
- ✅ Кнопка "Отправить на сборку" центрирована
- ✅ Нет ошибок "overflowed by XX pixels" в консоли

### Сценарий 3: Тестирование на разных размерах экрана
1. Протестировать модальное окно на:
   - Маленьком экране (напр. Galaxy A15, iPhone SE)
   - Среднем экране (напр. Pixel 6, iPhone 13)
   - Большом экране (напр. планшет)

**Ожидаемое поведение:**
- ✅ Все элементы корректно отображаются на всех размерах экрана
- ✅ Нет горизонтального или вертикального overflow
- ✅ Кнопки типов упаковки имеют одинаковую ширину (50% - padding)
- ✅ Текст в дропдауне тарифов не выходит за границы

---

## Технические детали

**Измененные файлы:**
- `lib/src/features/tracks/presentation/tracks_screen.dart`
- `pubspec.yaml` (версия)

**Новые зависимости**: Нет

**Breaking changes**: Нет

---

## Размеры сборки

- **AAB**: 57.1 MB
- **APK**: 75.9 MB

---

## Графические примеры изменений

### До (v1.0.0+4):
- FAB кнопка прижата к нижнему меню (24px отступ)
- GridView с типами упаковки вызывает overflow на маленьких экранах
- Длинные названия тарифов вызывают right overflow
- Кнопка "Отправить на сборку" занимает 100% ширины

### После (v1.0.0+5):
- FAB кнопка имеет визуальное разделение (29px отступ)
- Wrap с типами упаковки адаптируется под размер экрана
- Длинные названия обрезаются троеточием
- Кнопка "Отправить на сборку" 80% ширины, центрирована

---

## Примечания для разработчиков

### Использование Wrap вместо GridView
Wrap автоматически переносит элементы на новую строку, что предотвращает overflow на маленьких экранах. В отличие от GridView с фиксированным childAspectRatio, Wrap адаптируется под контент.

```dart
// Плохо - может вызвать overflow
GridView.count(
  crossAxisCount: 2,
  childAspectRatio: 3.0, // Фиксированное соотношение
  children: [...],
)

// Хорошо - адаптируется под экран
Wrap(
  spacing: 8,
  runSpacing: 8,
  children: items.map((item) => Container(
    constraints: BoxConstraints(
      minWidth: (screenWidth - padding) / 2,
      maxWidth: (screenWidth - padding) / 2,
    ),
    child: ...,
  )).toList(),
)
```

### Предотвращение overflow в Row/Column
Всегда оборачивайте текст в Expanded, если он находится в Row и может быть длинным:

```dart
// Плохо - может вызвать overflow
Row(
  children: [
    Column(
      children: [
        Text(longText), // Overflow!
      ],
    ),
    Icon(...),
  ],
)

// Хорошо - текст обрезается
Row(
  children: [
    Expanded(
      child: Column(
        children: [
          Text(
            longText,
            overflow: TextOverflow.ellipsis,
            maxLines: 1,
          ),
        ],
      ),
    ),
    Icon(...),
  ],
)
```
